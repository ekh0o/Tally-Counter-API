Tally Counter REST API

Requirements
NodeJS 18
Express 4.18.2
Winston 3.11.0

Installation:

npm install

Run server:

node src/main.js

Endpoints:

GET /counter-read
GET /counter-increase
GET /counter-reset

Logs are stored in logs/error.log and logs/combined.log